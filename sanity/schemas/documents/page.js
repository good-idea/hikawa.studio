import { pageFields } from './shared'
import { TiDocument } from 'react-icons/ti'

const page = {
	title: 'Pages',
	name: 'page',
	type: 'document',
	icon: TiDocument,
	fields: pageFields,
}

export default page
